APP_NAME=Study Group Matcher
# Persist DB under /data inside the container (mapped to a named volume)
DATABASE_URL=sqlite:////data/dev.db
# Set to 1 to run seed data on startup
SEED=0
# Uvicorn settings
HOST=0.0.0.0
PORT=8000
